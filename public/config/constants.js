const CONFIG = {
    API: {
        BASE_URL: window.location.origin,
        ENDPOINTS: {
            USER: '/api/user',
            BALANCE: '/api/balance',
            TRANSACTION: '/api/transaction',
            GIFT: '/api/gift'
        }
    }
};